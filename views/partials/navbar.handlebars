<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">TechShop</a>
    
    <div class="d-flex align-items-center">
      <a href="/products" class="btn btn-outline-light me-2">
        <i class="fas fa-store me-1"></i> Productos
      </a>
      
      {{#if cartId}}
        <a href="/carts/{{cartId}}" class="btn btn-outline-light position-relative">
          <i class="fas fa-shopping-cart me-1"></i>
          <span id="cart-counter" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
            0
          </span>
        </a>
      {{/if}}
    </div>
  </div>
</nav>